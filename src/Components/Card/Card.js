import React from 'react';
import CardFeaturs from './CardFeaturs';
import CardTop from './CardTop';

const Card = () => {
    return (
        <main>
            <CardTop/>
            <CardFeaturs/>
        </main>
    );
};

export default Card;